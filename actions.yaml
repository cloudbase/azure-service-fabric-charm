get-ad-user-credentials:
  description: |
    Returns the Active Directory credentials for both AD users requested
    by the charm. There are two AD users requested by the charm (one is granted
    with cluster administrative priviledges and the other one it is not).
