get-ad-user-credentials:
  description: |
    Returns the Active Directory credentials for both AD users requested
    by the charm. The requested AD users are a normal domain user and a
    domain admin user.
